spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false

spring.flyway.enabled=false

# Configuração mínima de auth para o contexto de testes carregar.
app.jwt.secret=test-secret-0123456789abcdef0123456789abcdef
app.cors.allowed-origins=http://localhost:3000

# Admin para o AdminUserSeeder rodar no contexto de testes; demo desligado.
app.admin.email=admin@test.local
app.admin.password=admin12345
app.seed.demo.enabled=false
