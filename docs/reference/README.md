# Referência

Documentação **estável** do que existe hoje: modelo de dados, endpoints da API,
contratos entre app e backend. Descreve o estado atual — não propostas (essas
vão para [`plans/`](../plans/)).

A API também é documentada em runtime via OpenAPI/Scalar (ver `backend/`).

## Índice

_(vazio — adicione referências aqui, ex.: `modelo-de-dados.md`, `api-endpoints.md`)_
